version: '3'

silent: true
vars:
  VAR: included-global-vars
  ENV: included-global-vars

tasks:
  default:
    dotenv:
      - 'task.env'
    vars:
      VAR: included-task-vars
      ENV: included-task-vars
    cmds:
      - 'echo "{{.VAR}}"'
      - 'echo "$ENV"'
