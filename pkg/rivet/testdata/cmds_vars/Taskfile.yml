version: '3'
tasks:
  build-checksum:
    cmds:
      - echo "{{.CHECKSUM}}"
    transforms:
      - matches:
          - ./source.txt
  build-ts:
    method: timestamp
    cmds:
      - echo '{{.TIMESTAMP.Unix}}'
      - echo '{{.TIMESTAMP}}'
    transforms:
      - matches:
          - ./source.txt
